name: Swift
on:
  push:
  pull_request:
jobs:
  swift-build-26:
    env:
      XCODE_VERSION: "26.0"
    runs-on: "macos-26"
    steps:
      - uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: ${{ env.XCODE_VERSION }}
      - uses: actions/checkout@v3
      - name: Download MetalToolchain
        run: xcodebuild -downloadComponent MetalToolchain
      - name: Trust plugins
        run: defaults write com.apple.dt.Xcode IDESkipPackagePluginFingerprintValidation -bool YES
      - name: Build (macOS)
        run: swift build -v
      - name: Test (macOS)
        run: swift test -v
      - name: Create visionOS destination
        run: |
          cat > .visionos-destination.json << 'EOF'
          {
              "version": 1,
              "sdk": "/Applications/Xcode.app/Contents/Developer/Platforms/XROS.platform/Developer/SDKs/XROS.sdk",
              "toolchain-bin-dir": "/Applications/Xcode.app/Contents/Developer/Toolchains/XcodeDefault.xctoolchain/usr/bin",
              "target": "arm64-apple-xros26.0",
              "extra-cc-flags": [],
              "extra-swiftc-flags": [],
              "extra-cpp-flags": []
          }
          EOF
      - name: Build (visionOS)
        run: swift build -v --destination .visionos-destination.json
      - name: Create iOS destination
        run: |
          cat > .ios-destination.json << 'EOF'
          {
              "version": 1,
              "sdk": "/Applications/Xcode.app/Contents/Developer/Platforms/iPhoneOS.platform/Developer/SDKs/iPhoneOS.sdk",
              "toolchain-bin-dir": "/Applications/Xcode.app/Contents/Developer/Toolchains/XcodeDefault.xctoolchain/usr/bin",
              "target": "arm64-apple-ios26.0",
              "extra-cc-flags": [],
              "extra-swiftc-flags": [],
              "extra-cpp-flags": []
          }
          EOF
      - name: Build (iOS)
        run: swift build -v --destination .ios-destination.json
      - name: Build Simple Demo (macOS)
        run: |
          xcodebuild build \
            -project Examples/MetalSprocketsGaussianSplatsDemo/MetalSprocketsGaussianSplatsDemo.xcodeproj \
            -scheme MetalSprocketsGaussianSplatsDemo \
            -destination 'platform=macOS' \
            -skipPackagePluginValidation \
            -skipMacroValidation \
            CODE_SIGNING_ALLOWED=NO
      - name: Build Simple Demo (iOS)
        run: |
          xcodebuild build \
            -project Examples/MetalSprocketsGaussianSplatsDemo/MetalSprocketsGaussianSplatsDemo.xcodeproj \
            -scheme MetalSprocketsGaussianSplatsDemo \
            -destination 'generic/platform=iOS' \
            -skipPackagePluginValidation \
            -skipMacroValidation \
            CODE_SIGNING_ALLOWED=NO
      - name: Build Simple Demo (visionOS)
        run: |
          xcodebuild build \
            -project Examples/MetalSprocketsGaussianSplatsDemo/MetalSprocketsGaussianSplatsDemo.xcodeproj \
            -scheme MetalSprocketsGaussianSplatsDemo \
            -destination 'generic/platform=xrOS' \
            -skipPackagePluginValidation \
            -skipMacroValidation \
            CODE_SIGNING_ALLOWED=NO
      - name: Build Super Demo (macOS)
        run: |
          xcodebuild build \
            -project Examples/MetalGaussianSplatsSuperDemo/MetalGaussianSplatsDemo.xcodeproj \
            -scheme MetalGaussianSplatsDemo \
            -destination 'platform=macOS' \
            -skipPackagePluginValidation \
            -skipMacroValidation \
            CODE_SIGNING_ALLOWED=NO
      - name: Build Super Demo (iOS)
        run: |
          xcodebuild build \
            -project Examples/MetalGaussianSplatsSuperDemo/MetalGaussianSplatsDemo.xcodeproj \
            -scheme MetalGaussianSplatsDemo \
            -destination 'generic/platform=iOS' \
            -skipPackagePluginValidation \
            -skipMacroValidation \
            CODE_SIGNING_ALLOWED=NO
      - name: Build Super Demo (visionOS)
        run: |
          xcodebuild build \
            -project Examples/MetalGaussianSplatsSuperDemo/MetalGaussianSplatsDemo.xcodeproj \
            -scheme MetalGaussianSplatsDemo \
            -destination 'generic/platform=xrOS' \
            -skipPackagePluginValidation \
            -skipMacroValidation \
            CODE_SIGNING_ALLOWED=NO
